#include "main.h"
/**
 * _abs - Computes absolute value of integer.
 * @n: Integer to be computed.
 *
 * Return: Absolute value of the integer.
 */
int _abs(int n)
{
	if (n >= 0)
		return ('n');
	else
		return (-n);
}
