#include "main.h"
/**
 * main - Checks for lowercase character
 * Return: Always 0
 */
int _islower(int c);
{
	for ('c') = alphabets
		if (c >= 'a' && c <= 'z')
		{
			_putchar('1');
		}
		else
		{
			_putchar('0');
		}
	return (0);
}
